<Window x:Class="Questionnaires.TeacherAuthorization"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Questionnaires"
        mc:Ignorable="d"
        Title="TeacherAuthorization" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <GroupBox Grid.Column="1" Margin="10" Header="Войти, как преподаватель"  Foreground="Purple" BorderBrush="#FF9416BB" FontSize="14" FontWeight="Bold">
            <StackPanel Name="AuthorizationForTeacher" Grid.Column="0"  Margin="10" Background="#FFE7F3F2" DataContext="{Binding NewSpeciality}">
                <Label>Укажите логин</Label>
                <TextBox  Margin="10" Name="textLogin"></TextBox>
                <Label>Укажите пароль</Label>
                <TextBox  Margin="10" Name="textPass"></TextBox>
                <Button Name="TeacherAuthorizationsssss" Click="AuthorizationForTeacherButton" Margin="10" Content="Войти"></Button>
            </StackPanel>
        </GroupBox>
    </Grid>
</Window>
